# ============================================================
# 阿里云 ECS 覆盖配置
# ============================================================
services:
  gezhe:
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "3"
