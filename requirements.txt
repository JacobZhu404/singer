# 运行时：Python 3.11（见 CLAUDE.md「环境与启动」）。请用项目 venv，勿用系统 python3(3.7)。
# 关键版本钉死：pandas 钉 2.2.x、numpy 钉 1.26.x，避开 pandas3/numpy2 的 breaking 改动。

# Web 服务（Flask，非 fastapi）
Flask>=3.0,<4.0
flask-cors>=4.0

# 数据/计算（指标基于 pandas/numpy 自实现，无 TA-Lib 依赖）
pandas==2.2.3
numpy==1.26.4
baostock>=0.9.0
requests>=2.31

# 测试
pytest>=7.0.0
